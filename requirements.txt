# Core DL stack. On Databricks, install torch/torchvision from the cluster's
# CUDA-matched wheels instead of pinning here; locally these resolve to CPU builds.
torch>=2.1
torchvision>=0.16
timm>=0.9.12

# Data / images
# numpy pinned <2: torch/pandas in this env are built against the 1.x ABI.
numpy>=1.26,<2
pandas>=2.1
Pillow>=10.0
# opencv 4.12+ declares a hard numpy>=2 requirement; 4.11.x works with numpy 1.x.
opencv-python-headless>=4.8,<4.12
albumentations>=1.4

# Metrics / EDA
scikit-learn>=1.3
matplotlib>=3.8

# Config
PyYAML>=6.0

# Optional: experiment tracking (native on Databricks). Code degrades gracefully
# if absent.
mlflow>=2.9
